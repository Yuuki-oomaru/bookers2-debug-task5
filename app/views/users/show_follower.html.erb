<div class="container">
  <div class="row">
    <div class="col-xs-9">
      <h1>Follower</h1>
      <% @user.followers.each do |user| %>
      <table>
      <tr>
        <td>  <%= user.name %> </td>
        <td><%= link_to '詳細', user_path(user) %></td>
      </tr>
      </table>
      <% end %>
    </div>
  </div>
</div>
