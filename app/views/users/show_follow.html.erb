<div class="container">
  <div class="row">
    <div class="col-xs-9">
      <h1>フォロー</h1>
      <% @user.followings.each do |user| %>
      <table>
      <tr>
        <td><%= user.name %></td>
        <td><%= link_to '詳細', user_path(user) %></td>
      </tr>
      </table>
      <% end %>
    </div>
  </div>
</div>
